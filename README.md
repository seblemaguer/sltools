# My helpers

I won't describe the content of the package, the key part is that one sub directory = one tool. See pyproject.toml for the list of commands.

## How to install

```sh
pip install -e .[dev]
```
